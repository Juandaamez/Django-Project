# Variables de entorno para producción (Vercel)
# Copia este archivo como .env.production y configura la URL de tu API

# URL del backend en Render (se configura después del deploy)
VITE_API_URL=https://django-project-bmc7.onrender.com
